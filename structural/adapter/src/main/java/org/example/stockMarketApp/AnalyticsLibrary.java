package org.example.stockMarketApp;

// Service: 3rd-party analytics library that only works with JSON data
public class AnalyticsLibrary {
    public void analyzeJsonData(String jsonData) {
        System.out.println("Analyzing stock data with the 3rd-party analytics library:");
        System.out.println(jsonData);
    }
}
